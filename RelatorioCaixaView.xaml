<Window x:Class="SistemaOficina.RelatorioCaixaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SistemaOficina"
        xmlns:helpers="clr-namespace:SistemaOficina.Helpers"
        mc:Ignorable="d"
        Title="Relatório de Caixa" Height="1123" Width="794" 
        WindowStyle="ToolWindow" WindowStartupLocation="CenterOwner" 
        Background="White" Foreground="#1C1C1C"
        FontFamily="Segoe UI">

    <Window.Resources>
        <helpers:ByteArrayToImageSourceConverter x:Key="ByteArrayToImageConverter" />
        <helpers:EntryTypeToSignConverter x:Key="EntryTypeToSignConverter" />
    </Window.Resources>

    <Grid x:Name="PrintableArea" Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0" Source="{Binding Configuracao.Logo, Converter={StaticResource ByteArrayToImageConverter}}" 
                   Width="100" Height="100" Stretch="Uniform" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                <TextBlock Text="{Binding Configuracao.NomeEmpresa}" FontSize="26" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="{Binding Configuracao.Endereco}" FontSize="12" Foreground="#566573" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding Configuracao.Telefone}" FontSize="12" Foreground="#566573"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="0,1,0,1" Padding="0,10" Margin="0,20,0,10">
            <StackPanel>
                <TextBlock Text="Relatório de Fluxo de Caixa" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding PeriodoRelatorio}" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding LancamentosDoPeriodo}" AutoGenerateColumns="False" 
                  IsReadOnly="True" CanUserAddRows="False" BorderThickness="0"
                  HeadersVisibility="Column" Background="Transparent" RowHeaderWidth="0"
                  HorizontalGridLinesBrush="#E0E0E0" VerticalGridLinesBrush="Transparent">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#34495E"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="Padding" Value="8,10"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    <Setter Property="BorderBrush" Value="#BDC3C7"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Padding" Value="0,5"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Binding="{Binding Data, StringFormat='dd/MM/yy HH:mm'}" Width="1.5*"/>
                <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="4*"/>
                <DataGridTextColumn Header="Técnico/Responsável" Binding="{Binding ResponsavelTransacao}" Width="2*"/>

                <DataGridTemplateColumn Header="Valor" Width="1.5*" SortMemberPath="Valor">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Right">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Tipo}" Value="Entrada">
                                                <Setter Property="Foreground" Value="#27AE60"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Tipo}" Value="Saida">
                                                <Setter Property="Foreground" Value="#C0392B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0}{1:C}">
                                        <Binding Path="Tipo" Converter="{StaticResource EntryTypeToSignConverter}"/>
                                        <Binding Path="Valor"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" BorderBrush="#BDC3C7" BorderThickness="0,2,0,0" Margin="0,20,0,0" Padding="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Resumo do Período" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="Total de Entradas:" HorizontalAlignment="Right" FontSize="12" Margin="0,0,15,5"/>
                    <TextBlock Text="Total de Saídas:" HorizontalAlignment="Right" FontSize="12" Margin="0,0,15,5"/>
                    <TextBlock Text="Saldo do Período:" HorizontalAlignment="Right" FontSize="14" FontWeight="Bold" Margin="0,10,15,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding TotalEntradas, StringFormat=C}" Foreground="#27AE60" FontWeight="SemiBold" HorizontalAlignment="Right" FontSize="12" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding TotalSaidas, StringFormat=C}" Foreground="#C0392B" FontWeight="SemiBold" HorizontalAlignment="Right" FontSize="12" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding SaldoPeriodo, StringFormat=C}" Foreground="#2C3E50" FontWeight="Bold" HorizontalAlignment="Right" FontSize="14" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock Grid.Row="4" Text="{Binding DataGeracaoRelatorio}" FontSize="10" Foreground="Gray" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
    </Grid>
</Window>