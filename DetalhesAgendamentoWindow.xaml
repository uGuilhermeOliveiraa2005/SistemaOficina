<Window x:Class="SistemaOficina.DetalhesAgendamentoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SistemaOficina"
        mc:Ignorable="d"
        Title="Detalhes do Agendamento" 
        MinWidth="600"
        Width="650"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        Background="#F4F6F7"
        SizeToContent="Height" 
        MaxHeight="600">
    <Window.Resources>
        <Style TargetType="Button" x:Key="BaseButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="30,12"/>
            <Setter Property="Margin" Value="10,20,0,0"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=BorderBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="FecharButton" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#3498DB"/>
            <Setter Property="BorderBrush" Value="#3498DB"/>
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="Margin" Value="0,15,0,5"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style x:Key="DataStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
    </Window.Resources>
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Agendamento para " FontSize="26" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding NomeClienteDisplay}" FontSize="26" FontWeight="Normal" Foreground="#2C3E50" VerticalAlignment="Center"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitle}" Text="Informações Gerais"/>
                <Separator Background="#E0E0E0"/>
                <Grid Margin="0,10,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Data e Hora:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Style="{StaticResource DataStyle}" Text="{Binding DataAgendamento, StringFormat={}{0:dd/MM/yyyy 'às' HH:mm}}" FontWeight="SemiBold" Margin="0,5,25,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Equipamento:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Style="{StaticResource DataStyle}" Text="{Binding Equipamento}" Margin="0,5,25,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Status:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Style="{StaticResource DataStyle}" Text="{Binding Status}" Foreground="#E67E22" FontWeight="Bold" Margin="0,5,25,5"/>
                </Grid>
                <TextBlock Style="{StaticResource SectionTitle}" Text="Dados do Cliente e Coleta"/>
                <Separator Background="#E0E0E0"/>
                <Grid Margin="0,10,0,10" x:Name="gridDadosCliente">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Tipo de Cliente:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Style="{StaticResource DataStyle}" x:Name="txtTipoCliente" Margin="0,5,0,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Telefone:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Style="{StaticResource DataStyle}" x:Name="txtTelefoneCliente" Text="Não informado" Margin="0,5,0,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Endereço:" Margin="0,10,15,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Style="{StaticResource DataStyle}" x:Name="txtEnderecoCompleto" Text="Não informado" Margin="0,10,0,5"/>
                    <TextBlock Grid.Row="3" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Número:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Style="{StaticResource DataStyle}" x:Name="txtNumero" Text="Não informado" Margin="0,5,0,5"/>
                    <TextBlock Grid.Row="4" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Bairro:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Style="{StaticResource DataStyle}" x:Name="txtBairro" Text="Não informado" Margin="0,5,0,5"/>
                    <TextBlock Grid.Row="5" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="Cidade:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" Style="{StaticResource DataStyle}" x:Name="txtCidade" Text="Não informado" Margin="0,5,0,5"/>
                    <TextBlock Grid.Row="6" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="UF:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="6" Grid.Column="1" Style="{StaticResource DataStyle}" x:Name="txtUf" Text="Não informado" Margin="0,5,0,5"/>
                    <TextBlock Grid.Row="7" Grid.Column="0" Style="{StaticResource LabelStyle}" Text="CEP:" Margin="0,5,15,5"/>
                    <TextBlock Grid.Row="7" Grid.Column="1" Style="{StaticResource DataStyle}" x:Name="txtCep" Text="Não informado" Margin="0,5,0,5"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Fechar" Click="Fechar_Click" Style="{StaticResource FecharButton}"/>
        </StackPanel>
    </Grid>
</Window>