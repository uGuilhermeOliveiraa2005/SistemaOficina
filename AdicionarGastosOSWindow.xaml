<Window x:Class="SistemaOficina.AdicionarGastosOSWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SistemaOficina"
        mc:Ignorable="d"
        Title="Adicionar Gastos da OS" Height="550" Width="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        Background="#F4F6F7">
    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#D0D3D4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#AABBC3"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3498DB"/>
                                <Setter Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Foreground" Value="#3498DB"/>
            <Setter Property="BorderBrush" Value="#3498DB"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2ECC71"/>
            <Setter Property="BorderBrush" Value="#2ECC71"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="22" FontWeight="Bold" Margin="0,0,0,15" Foreground="#2C3E50">
            <Run Text="Adicionar Gastos para a OS Nº"/>
            <Run x:Name="runNumeroOS" FontWeight="Normal" Foreground="#3498DB"/>
        </TextBlock>

        <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="20" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Descrição do Gasto:" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtDescricaoGasto" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,10,0">
                    <TextBlock Text="Valor (R$):" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtValorGasto" Style="{StaticResource ModernTextBox}" PreviewTextInput="Valor_PreviewTextInput"/>
                </StackPanel>

                <Button x:Name="btnAdicionarGasto" Grid.Column="2" Content="Adicionar" VerticalAlignment="Bottom" Click="btnAdicionarGasto_Click"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="dgGastosAdicionados" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="3*"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <TextBlock x:Name="txtTotalGastos" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16" FontWeight="Bold">
                <Run Text="Total dos Gastos:"/>
                <Run Text="R$ 0,00"/>
            </TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnCancelar" Content="Cancelar" Click="btnCancelar_Click"/>
                <Button x:Name="btnConcluir" Content="Concluir e Fechar OS" Style="{StaticResource SuccessButton}" Click="btnConcluir_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>